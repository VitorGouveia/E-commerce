import { auth } from './lib/auth';

export default auth;

const {
	access_token,
	admin_access,
	admin_refresh,
	loginEmail,
	loginUsername,
	refresh_token,
	verify,
} = auth;

export {
	access_token,
	admin_access,
	admin_refresh,
	loginEmail,
	loginUsername,
	refresh_token,
	verify,
};
